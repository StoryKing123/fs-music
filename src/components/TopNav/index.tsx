import React from "react";
import { FC } from "react";
import Search from "../Search";

const TopNav: FC = () => {
    return (
        <div>
            nav
            <Search></Search>
        </div>
    );
};

export default TopNav;
